{
  "private": true,
  "scripts": {
    "pretest": "cd src/Tests; dotnet fable npm-run rollup",
    "rollup": "rollup -c",
    "test": "mocha build"
  },
  "devDependencies": {
    "babel-core": "^6.25.0",
    "fable-loader": "^1.0.6",
    "mocha": "^3.4.2",
    "rollup": "^0.45.2",
    "rollup-plugin-fable": "^1.0.7"
  }
}
